import { createAction } from 'typesafe-actions';

/* XXX */

export const xxx = createAction('{{ titleCase name }} / XXX / Loading', resolve =>
  resolve,
);

export const xxxSuccess = createAction('{{ titleCase name }} / XXX / Success', resolve =>
  resolve,
);

export const xxxError = createAction('{{ titleCase name }} / XXX / Error', resolve =>
  resolve,
);
